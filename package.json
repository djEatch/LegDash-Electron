{
  "name": "electron-leg-dash",
  "version": "3.2.5",
  "description": "electron application to report current leg assignments for a given list of servers",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . LegDash3 --overwrite --platform=win32 --arch=x64 --prune=true --out=release-builds --version-string.CompanyName=DJE --version-string.FileDescription=DJE --version-string.ProductName=\"Leg Dashboard Electron App\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "electron"
  ],
  "author": "DJEatch",
  "license": "ISC",
  "devDependencies": {
    "electron": ">=2.0.8"
  },
  "dependencies": {
    "bootstrap": ">=4.1.2",
    "cryptiles": ">=4.1.2",
    "jquery": "^3.3.1",
    "popper.js": "^1.14.6"
  }
}
